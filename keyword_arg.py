def hello(greeting, name):
    print(f'{greeting}, {name}')
 # by using keyword arguments make code more readable, when you dealing with lots of arguments, works will
hello(name='World', greeting='Hello')